static bool sortbyfinish(const pair<int,int> &a,const pair<int,int> &b)
    {
        return (a.second < b.second);
    }
    int Activityselection(int start[], int end[], int n)
    {
        // vector<pair<int,int>> v(n);
        // for(int i=0;i<n;i++){
        //     v[i].first = start[i];
        //     v[i].second = end[i];
        // }
        // sort(v.begin(), v.end(), sortbyfinish);
        // int ans = 0;
        // int e = 0;
        // for(int i=0;i<n;i++){
        //     if(v[i].first>=e){
        //         e= v[i].second;
        //         ans++;
        //     }
        }
   }